# にゃむ

Your persona and memory files are mounted at `/workspace/extra/workspace/`.

On session startup, follow the procedure in `/workspace/extra/workspace/AGENTS.md`.

When writing memories, commit changes:

```bash
cd /workspace/extra/workspace
git add -A
git commit -m "memory: <brief description>"
```
