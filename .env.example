# Default runtime: Docker (existing behavior).
# Set to "none" only when NanoClaw itself is already running inside an isolated VM sandbox
# such as Islo and you want child agents to run as local processes instead of nested Docker.
# NANOCLAW_CONTAINER_RUNTIME=docker

# Optional: override where the credential proxy binds.
# CREDENTIAL_PROXY_HOST=127.0.0.1
