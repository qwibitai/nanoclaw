# Constituency Bot Environment Variables
# Copy this file to .env and fill in the values.

# Claude Code OAuth token from your Claude Code subscription.
# Get this from: claude /settings -> Authentication -> Copy OAuth Token
# This is used by agent containers to authenticate with the Claude API
# at zero per-token cost (subscription-based billing).
CLAUDE_CODE_OAUTH_TOKEN=

# Optional: Anthropic API key (alternative to OAuth token)
# Only needed if not using Claude Code subscription.
# ANTHROPIC_API_KEY=

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info
