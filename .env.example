# Anthropic API (direct connection, not proxied)
ANTHROPIC_API_KEY=

# OneCLI Cloud vault (app.onecli.sh) — manages Discord, Resend, and other service credentials
# Service channels route through OneCLI proxy for credential injection + policy enforcement
ONECLI_API_KEY=

ASSISTANT_NAME=Nexus
GATEWAY_PORT=3001
GATEWAY_URL=http://localhost:3001
OPERATOR_SLUG=foundry
OPERATOR_NAME=Microgrid Foundry
