# ============================================================
# NanoClaw — environment variables
# Copy to .env and fill in values before starting.
# ============================================================

# ── Core ────────────────────────────────────────────────────
# Your Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# Trigger word users type to address the assistant (default: Andy)
ASSISTANT_NAME=Andy

# Set to true if the bot has its own dedicated WhatsApp number.
# When false, outgoing messages are prefixed with the assistant name.
ASSISTANT_HAS_OWN_NUMBER=false

# ── Railway / Cloud deployment ───────────────────────────────
# Railway sets PORT automatically. The app starts an HTTP server
# on this port, exposing:
#   GET /health         — health check (always 200)
#   GET /qr?token=...   — WhatsApp QR code page for first-time auth
#PORT=3000

# Protect the /qr endpoint with a secret token.
# Visit /qr?token=YOUR_TOKEN to scan the QR code on first deploy.
#ADMIN_TOKEN=change-me

# ── Container / Agent ────────────────────────────────────────
# Docker image used for agent containers (default: nanoclaw-agent:latest)
#CONTAINER_IMAGE=nanoclaw-agent:latest

# Set to 1 after the first Railway deploy to skip rebuilding the agent
# image on every restart (speeds up restarts after initial setup).
#SKIP_CONTAINER_BUILD=

# Agent execution timeout in ms (default: 30 minutes)
#CONTAINER_TIMEOUT=1800000

# Max concurrent agent containers (default: 5)
#MAX_CONCURRENT_CONTAINERS=5

# ── Scheduling ───────────────────────────────────────────────
# Timezone for cron expressions (default: system timezone)
#TZ=Asia/Jerusalem

# ── Logging ─────────────────────────────────────────────────
# Log level: trace | debug | info | warn | error | fatal (default: info)
#LOG_LEVEL=info
