# NanoClaw 볼륨 마운트 가이드

> **읽기 전 규칙**  
> 이 문서에서 경로는 항상 두 가지로 구분합니다.  
> - 🖥️ **HOST** = NanoClaw가 설치된 서버/PC (파일이 실제로 저장되는 곳)  
> - 📦 **CONTAINER** = Docker가 실행한 격리 환경 (Claude Code가 돌아가는 곳)

---

## 왜 이 구분이 중요한가?

NanoClaw는 **Bind Mount** 방식을 사용합니다.

```
🖥️ HOST 특정 폴더  ←──────── 연결(bind) ────────→  📦 CONTAINER 경로
  (파일이 실제로 여기 저장됨)                         (Claude는 여기로 접근)
```

- 파일의 **실체**는 항상 HOST에 있습니다
- CONTAINER 안에서 파일을 만들거나 수정해도 **실제로는 HOST에 저장**됩니다
- 컨테이너가 종료(`--rm`)되어도 HOST 파일은 **그대로 남습니다**

---

## 전체 마운트 연결 지도

```
🖥️ HOST (나노클로 설치 경로)               📦 CONTAINER (Claude 실행 환경)
═══════════════════════════════         ═══════════════════════════════

[메인 그룹만]
{projectRoot}/                ──(읽기전용)──▶  /workspace/project/

[비메인 그룹만]
groups/global/                ──(읽기전용)──▶  /workspace/global/

[모든 그룹 공통]
groups/{그룹폴더}/             ──(읽기+쓰기)─▶  /workspace/group/
data/sessions/{그룹}/.claude/ ──(읽기+쓰기)─▶  /home/node/.claude/
data/ipc/{그룹}/              ──(읽기+쓰기)─▶  /workspace/ipc/
data/sessions/{그룹}/         ──(읽기+쓰기)─▶  /app/src/
  agent-runner-src/

[사용자가 추가한 경우]
~/projects/my-app/            ──(읽기+쓰기)─▶  /workspace/extra/my-app/
~/Documents/work/             ──(읽기전용)──▶  /workspace/extra/work/
```

---

## 마운트 1: 그룹 작업 폴더

```
🖥️ HOST                              📦 CONTAINER
─────────────────────                ─────────────────
groups/main/                  ────▶  /workspace/group/    (메인 그룹)
groups/dev-team/              ────▶  /workspace/group/    (dev-team 그룹)
groups/family/                ────▶  /workspace/group/    (family 그룹)
```

- **권한**: 읽기+쓰기
- **Claude가 기억을 저장하는 곳**: `CLAUDE.md` 파일을 여기에 씁니다
- **HOST에서 편집 가능**: 텍스트 에디터로 직접 `groups/main/CLAUDE.md`를 수정하면 Claude가 다음 실행 시 반영합니다
- 각 그룹은 자신의 폴더만 볼 수 있습니다 (다른 그룹 폴더는 접근 불가)

**HOST 폴더 구조 예시**:
```
🖥️ groups/
  main/
    CLAUDE.md          ← Claude의 지속 기억 (메인 그룹용)
    logs/              ← 컨테이너 실행 로그 파일들
  dev-team/
    CLAUDE.md          ← Claude의 지속 기억 (개발팀용)
    logs/
```

---

## 마운트 2: 전역 공용 기억 (비메인 그룹만)

```
🖥️ HOST                              📦 CONTAINER
─────────────────────                ─────────────────
groups/global/                ────▶  /workspace/global/
```

- **권한**: 읽기전용 (비메인 그룹은 수정 불가)
- **용도**: 어시스턴트 이름, 공통 행동 지침 등 모든 그룹이 공유하는 지침
- **메인 그룹**: `/workspace/project/groups/global/` 으로 이미 접근 가능하므로 별도 마운트 없음

---

## 마운트 3: 프로젝트 루트 (메인 그룹만)

```
🖥️ HOST                              📦 CONTAINER
─────────────────────                ─────────────────
{projectRoot}/                ────▶  /workspace/project/
(nanoclaw 설치 경로 전체)
```

- **권한**: 읽기전용 (쓰기 불가)
- **메인 그룹만 접근**: 관리자 그룹이므로 전체 소스코드, 설정을 볼 수 있음
- **읽기전용인 이유**: 만약 쓰기가 허용되면 Claude가 `src/`, `dist/`를 수정해 NanoClaw 자체를 변조할 수 있음

---

## 마운트 4: Claude 세션 + 스킬

```
🖥️ HOST                              📦 CONTAINER
─────────────────────                ─────────────────
data/sessions/main/.claude/   ────▶  /home/node/.claude/   (메인 그룹)
data/sessions/dev-team/.claude/ ──▶  /home/node/.claude/   (dev-team 그룹)
```

- **권한**: 읽기+쓰기
- **그룹별로 완전히 분리**: 메인과 dev-team은 서로 다른 HOST 폴더를 사용

**HOST 폴더 구조 (data/sessions/dev-team/.claude/)**:
```
🖥️ data/sessions/dev-team/.claude/
  settings.json          ← NanoClaw가 자동 생성 (Claude Code 설정)
  skills/                ← Claude Code 스킬 파일들
    agent-browser/
      SKILL.md           ← container/skills/ 에서 매 시작 시 복사
    my-custom-skill/     ← 직접 추가한 스킬 (보존됨)
      SKILL.md
  projects/              ← Claude Code 세션 ID 자동 저장
  memory/                ← Claude 자동 메모리 기록
```

**📦 CONTAINER에서 보이는 경로**:
```
📦 /home/node/.claude/
  settings.json
  skills/
    agent-browser/SKILL.md
  projects/
  memory/
```

### 스킬(Skill) 파일 흐름

```
🖥️ HOST 원본                         🖥️ HOST 그룹별 복사               📦 CONTAINER
container/skills/                data/sessions/{그룹}/.claude/    /home/node/.claude/
  agent-browser/                   skills/                          skills/
    SKILL.md          ──복사──▶      agent-browser/     ──마운트──▶    agent-browser/
                     (매 시작 시)      SKILL.md                          SKILL.md
```

> 스킬을 영구적으로 추가하려면 🖥️ **HOST**의 `container/skills/` 에 폴더를 만드세요.  
> CONTAINER 내부에서 직접 스킬을 추가하면 컨테이너 재시작 시 살아있지만, `container/skills/`과 같은 이름이면 덮어씌워집니다.

---

## 마운트 5: IPC 통신 디렉토리

```
🖥️ HOST                              📦 CONTAINER
─────────────────────                ─────────────────
data/ipc/main/                ────▶  /workspace/ipc/   (메인 그룹)
data/ipc/dev-team/            ────▶  /workspace/ipc/   (dev-team 그룹)
```

- **권한**: 읽기+쓰기
- **용도**: 호스트와 컨테이너가 파일을 통해 메시지를 주고받는 통신 채널

```
🖥️ HOST: data/ipc/dev-team/         📦 CONTAINER: /workspace/ipc/
  input/                              input/
    {timestamp}.json    ◀── 호스트가 씀 / 컨테이너가 읽고 삭제
    _close              ◀── 호스트가 씀 (종료 신호)

  messages/
    {timestamp}.json    ──▶ 컨테이너가 씀 / 호스트가 읽고 삭제
                            (Claude의 send_message MCP 도구 사용 시)

  tasks/
    {timestamp}.json    ──▶ 컨테이너가 씀 / 호스트가 읽고 삭제
                            (schedule_task, register_group MCP 도구 사용 시)

  current_tasks.json    ◀── 호스트가 씀 (컨테이너가 list_tasks로 읽음)
  available_groups.json ◀── 호스트가 씀 (메인 그룹만 접근)
```

---

## 마운트 6: Agent-Runner 소스

```
🖥️ HOST                                      📦 CONTAINER
─────────────────────────────────           ─────────────────
data/sessions/main/agent-runner-src/  ────▶  /app/src/
data/sessions/dev-team/agent-runner-src/ ──▶  /app/src/
```

- **권한**: 읽기+쓰기
- **용도**: 그룹별로 Claude의 도구 설정을 다르게 할 수 있는 TypeScript 소스
- **최초 1회 복사**: `container/agent-runner/src/` → `data/sessions/{그룹}/agent-runner-src/`
  이후로는 복사하지 않으므로 그룹별 커스터마이징이 유지됨
- **컨테이너 시작 시**: `entrypoint.sh`가 `/app/src`를 `npm run build`로 재컴파일

---

## 마운트 7: 사용자 추가 마운트 (선택)

```
🖥️ HOST                              📦 CONTAINER
─────────────────────                ─────────────────
~/projects/my-app/            ────▶  /workspace/extra/my-app/
~/Documents/work/             ────▶  /workspace/extra/work/
```

### 설정 구조

**2가지 보안 계층:**

1. **allowlist 화이트리스트** - `~/.config/nanoclaw/mount-allowlist.json`
   - 프로젝트 밖 저장 (컨테이너에 절대 마운트되지 않음)
   - 마운트 가능한 최상위 경로 정의
   - 차단 패턴 정의
   - 비메인 그룹 읽기전용 옵션

2. **그룹별 마운트 설정** - SQLite DB `registered_groups.container_config`
   - 특정 그룹이 사용할 실제 마운트 정의
   - allowlist 범위 내에서만 설정 가능

### Allowlist 파일 구조

파일: `~/.config/nanoclaw/mount-allowlist.json`

```json
{
  "allowedRoots": [
    {
      "path": "~/projects",
      "allowReadWrite": true,
      "description": "개발 프로젝트"
    },
    {
      "path": "~/Documents",
      "allowReadWrite": false,
      "description": "문서 (읽기전용)"
    }
  ],
  "blockedPatterns": [
    "password",
    "secret",
    "token"
  ],
  "nonMainReadOnly": true
}
```

**검증 플로우:**

```
요청된 마운트
    ↓
hostPath가 실제로 존재?
    ↓ No → ❌ 거부
    ↓ Yes
hostPath가 allowedRoots 범위 내?
    ↓ No → ❌ 거부
    ↓ Yes
hostPath가 blockedPatterns 포함?
    ↓ Yes → ❌ 거부
    ↓ No
containerPath가 유효? (.. 불포함, 상대경로)
    ↓ No → ❌ 거부
    ↓ Yes
비메인 그룹 & nonMainReadOnly=true?
    ↓ Yes → 읽기전용 강제
    ↓ No
✅ 마운트 허가
```

### 기본 차단 패턴 (항상)

```
.ssh, .gnupg, .gpg, .aws, .azure, .gcloud, .kube, .docker,
credentials, .env, .netrc, .npmrc, .pypirc,
id_rsa, id_ed25519, private_key, .secret
```

### 컨테이너에서의 경로

allowlist를 통과한 마운트는 컨테이너에서 다음 경로로 보입니다:

```
/workspace/extra/{containerPath}/
```

예: `hostPath: ~/projects/my-app`, `containerPath: my-app`
→ 컨테이너에서: `/workspace/extra/my-app/`

---

## 요약: HOST에서 직접 편집 가능한 항목들

아래 항목들은 🖥️ **HOST 파일시스템에서 직접 편집**하면 다음 컨테이너 실행 시 반영됩니다.

| 편집 대상              | 🖥️ HOST 경로                                           | 📦 CONTAINER 경로                            | 반영 시점                      |
| ---------------------- | ----------------------------------------------------- | ------------------------------------------- | ------------------------------ |
| 그룹별 지시사항/기억   | `groups/{폴더}/CLAUDE.md`                             | `/workspace/group/CLAUDE.md`                | 즉시 (다음 대화부터)           |
| 전역 공통 지시사항     | `groups/global/CLAUDE.md`                             | `/workspace/global/CLAUDE.md`               | 즉시                           |
| 스킬 추가 (전 그룹)    | `container/skills/{스킬}/SKILL.md`                    | `/home/node/.claude/skills/{스킬}/SKILL.md` | 다음 컨테이너 시작 시          |
| 스킬 추가 (특정 그룹)  | `data/sessions/{폴더}/.claude/skills/{스킬}/SKILL.md` | `/home/node/.claude/skills/{스킬}/SKILL.md` | 다음 컨테이너 시작 시          |
| Claude Code 설정       | `data/sessions/{폴더}/.claude/settings.json`          | `/home/node/.claude/settings.json`          | 다음 컨테이너 시작 시          |
| agent-runner 도구 설정 | `data/sessions/{폴더}/agent-runner-src/index.ts`      | `/app/src/index.ts`                         | 다음 컨테이너 시작 시 (재빌드) |

---

*최종 업데이트: 2026-02-25*  
*참고 소스: `src/container-runner.ts` (buildVolumeMounts 함수)*
