---
phase: 02-attachments
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/channels/websocket.ts
  - src/channels/websocket.test.ts
autonomous: true
requirements: [ATT-01, ATT-02, ATT-03, CONF-03]

must_haves:
  truths:
    - "Un mensaje WS con attachments[] base64 guarda los archivos en groups/better-work/inbox/attachments/ y el agente recibe la referencia en el content"
    - "Un archivo en groups/better-work/files/ es accesible via HTTP GET en WEBSOCKET_FILES_PORT (default 3002)"
    - "Cuando el agente incluye 'files/nombre.pdf' en su respuesta, el mensaje WS saliente incluye attachments:[{name, url}]"
    - "WEBSOCKET_FILES_PORT es configurable via env var con default 3002"
    - "El servidor HTTP no crashea NanoClaw si el puerto ya está en uso (log warn + continuar)"
    - "Path traversal en la URL HTTP devuelve 403 (nunca sirve archivos fuera de files/)"
  artifacts:
    - path: "src/config.ts"
      provides: "WEBSOCKET_FILES_PORT constant exported"
      contains: "WEBSOCKET_FILES_PORT"
    - path: "src/channels/websocket.ts"
      provides: "Inbound attachment saving, HTTP static server, outbound attachment detection"
      contains: "saveInboundAttachment"
    - path: "src/channels/websocket.test.ts"
      provides: "Tests for ATT-01, ATT-02, ATT-03"
      min_lines: 120
  key_links:
    - from: "src/channels/websocket.ts handleInboundMessage"
      to: "groups/better-work/inbox/attachments/"
      via: "saveInboundAttachment() + fs.writeFileSync"
      pattern: "writeFileSync.*inbox.*attachments"
    - from: "src/channels/websocket.ts connect()"
      to: "http.createServer"
      via: "startFileServer() called in connect()"
      pattern: "http\\.createServer"
    - from: "src/channels/websocket.ts sendMessage()"
      to: "attachments[] in WS payload"
      via: "extractOutboundAttachments() + regex scan"
      pattern: "extractOutboundAttachments"
---

<objective>
Extender WebSocketChannel con soporte de adjuntos bidireccional y servidor HTTP estático para archivos del agente.

Purpose: El panel web puede enviar archivos al agente (base64) y el agente puede compartir archivos de vuelta via URL HTTP.
Output: config.ts con WEBSOCKET_FILES_PORT; websocket.ts con saveInboundAttachment, startFileServer, extractOutboundAttachments; tests cubriendo ATT-01, ATT-02, ATT-03.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-attachments/02-CONTEXT.md
@.planning/phases/02-attachments/02-RESEARCH.md

<interfaces>
<!-- Interfaces relevantes del código existente. El executor NO necesita explorar el codebase. -->

From src/config.ts (patrón a seguir para CONF-03):
```typescript
// Patrón existente — replicar para WEBSOCKET_FILES_PORT
const envConfig = readEnvFile([
  'WEBSOCKET_ENABLED',
  'WEBSOCKET_PORT',
  // añadir 'WEBSOCKET_FILES_PORT' aquí
]);

export const WEBSOCKET_PORT = parseInt(
  process.env.WEBSOCKET_PORT || envConfig.WEBSOCKET_PORT || '3001',
  10,
);
// Añadir justo después:
export const WEBSOCKET_FILES_PORT = parseInt(
  process.env.WEBSOCKET_FILES_PORT || envConfig.WEBSOCKET_FILES_PORT || '3002',
  10,
);
```

From src/channels/websocket.ts (estado actual — Phase 1):
```typescript
export interface WebSocketChannelOpts {
  onMessage: OnInboundMessage;
  onChatMetadata: OnChatMetadata;
}

export class WebSocketChannel implements Channel {
  name = 'websocket';
  private wss: WebSocketServer | null = null;
  private client: WebSocket | null = null;
  private buffer: BufferedMessage[] = [];
  private heartbeatInterval: ReturnType<typeof setInterval> | null = null;
  private pongTimeout: ReturnType<typeof setTimeout> | null = null;

  constructor(
    private readonly port: number,
    private readonly opts: WebSocketChannelOpts,
  ) {}

  // connect(), handleInboundMessage(), sendMessage(), bufferMessage(),
  // flushBuffer(), isConnected(), ownsJid(), setTyping(), disconnect()
  // — todos implementados, ver archivo completo
}
```

From src/channels/websocket.test.ts (patrón de mock — reutilizar):
```typescript
vi.mock('../config.js', () => ({}));
vi.mock('../logger.js', () => ({
  logger: { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn() },
}));

// WSS mock (CRÍTICO: usar function regular, no arrow — soporta new())
vi.mock('ws', () => {
  function WSS(this: unknown) {
    return {
      on: (event: string, handler: (...args: unknown[]) => void) => {
        if (event === 'connection') connectionHandler = handler;
      },
      close: vi.fn((cb?: () => void) => cb?.()),
    };
  }
  const WS = { OPEN: 1 };
  return { WebSocketServer: vi.fn().mockImplementation(WSS), WebSocket: WS };
});

function createFakeClient() {
  const ee = new EventEmitter();
  return {
    readyState: 1,
    send: vi.fn(),
    terminate: vi.fn(),
    ping: vi.fn(),
    on: (e: string, h: (...args: unknown[]) => void) => ee.on(e, h),
    _emit: (e: string, ...args: unknown[]) => ee.emit(e, ...args),
  };
}
```

From src/types.ts (Channel interface — NO modificar):
```typescript
export interface Channel {
  name: string;
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  sendMessage(jid: string, text: string): Promise<void>;
  isConnected(): boolean;
  ownsJid(jid: string): boolean;
  setTyping(jid: string, isTyping: boolean): Promise<void>;
}
// sendMessage(jid, text) — NO cambia la firma
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir WEBSOCKET_FILES_PORT a config y crear directorios de adjuntos</name>
  <files>src/config.ts</files>
  <action>
Dos cambios en src/config.ts:

1. Añadir 'WEBSOCKET_FILES_PORT' al array del readEnvFile call (junto a 'WEBSOCKET_PORT').

2. Añadir la constante exportada justo después de WEBSOCKET_PORT:
```typescript
export const WEBSOCKET_FILES_PORT = parseInt(
  process.env.WEBSOCKET_FILES_PORT || envConfig.WEBSOCKET_FILES_PORT || '3002',
  10,
);
```

No hay cambios en otros archivos en esta tarea. Los directorios groups/better-work/inbox/attachments/ y groups/better-work/files/ se crean on-demand en websocket.ts (Task 2), no aquí.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>WEBSOCKET_FILES_PORT está exportado desde src/config.ts. El build de TypeScript no reporta errores.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar adjuntos entrantes + HTTP estático + adjuntos salientes en WebSocketChannel</name>
  <files>src/channels/websocket.ts</files>
  <action>
Modificar src/channels/websocket.ts con los siguientes cambios. NO modificar la interfaz Channel ni la firma de sendMessage.

**Imports a añadir** (al inicio del archivo, junto a los existentes):
```typescript
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { GROUPS_DIR } from '../config.js';
import { WEBSOCKET_FILES_PORT } from '../config.js';
```

**Tipos nuevos** (añadir junto a BufferedMessage):
```typescript
interface InboundAttachment {
  name: string;
  data: string;   // base64
  mime: string;
  size: number;
}
```

**Propiedades privadas nuevas** en la clase (añadir junto a pongTimeout):
```typescript
private fileServer: http.Server | null = null;
private readonly groupDir: string;
private readonly filesDir: string;
```

**Constructor** — actualizar para inicializar groupDir y filesDir:
```typescript
constructor(
  private readonly port: number,
  private readonly opts: WebSocketChannelOpts,
  private readonly filesPort: number = WEBSOCKET_FILES_PORT,
) {
  this.groupDir = path.join(GROUPS_DIR, 'better-work');
  this.filesDir = path.join(this.groupDir, 'files');
}
```

**Método saveInboundAttachment** (privado, añadir antes de connect()):
```typescript
private saveInboundAttachment(att: InboundAttachment): string {
  const inboxDir = path.join(this.groupDir, 'inbox', 'attachments');
  fs.mkdirSync(inboxDir, { recursive: true });

  const prefix = `${Date.now()}-${crypto.randomBytes(2).toString('hex')}`;
  const safeName = path.basename(att.name).replace(/[^a-zA-Z0-9._-]/g, '_');
  const filename = `${prefix}-${safeName}`;
  const dest = path.join(inboxDir, filename);

  const buffer = Buffer.from(att.data, 'base64');
  if (att.size > 0 && buffer.length !== att.size) {
    logger.warn({ name: att.name, expected: att.size, got: buffer.length }, 'Attachment size mismatch — file may be truncated');
  }
  fs.writeFileSync(dest, buffer);

  return path.join('inbox', 'attachments', filename);
}
```

**Método startFileServer** (privado, añadir junto a saveInboundAttachment):
```typescript
private startFileServer(): void {
  fs.mkdirSync(this.filesDir, { recursive: true });

  this.fileServer = http.createServer((req, res) => {
    if (!req.url?.startsWith('/files/')) {
      res.writeHead(404);
      res.end();
      return;
    }

    const relativePath = req.url.slice('/files/'.length);
    const resolved = path.resolve(this.filesDir, relativePath);
    if (!resolved.startsWith(path.resolve(this.filesDir) + path.sep) &&
        resolved !== path.resolve(this.filesDir)) {
      res.writeHead(403);
      res.end();
      return;
    }

    if (!fs.existsSync(resolved) || fs.statSync(resolved).isDirectory()) {
      res.writeHead(404);
      res.end();
      return;
    }

    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Content-Type', this.getMimeType(path.extname(resolved)));
    fs.createReadStream(resolved).pipe(res);
  });

  this.fileServer.on('error', (err: NodeJS.ErrnoException) => {
    if (err.code === 'EADDRINUSE') {
      logger.warn({ port: this.filesPort }, 'Files port already in use — HTTP server not started');
    } else {
      logger.error({ err }, 'File server error');
    }
  });

  this.fileServer.listen(this.filesPort, '127.0.0.1');
}
```

**Método getMimeType** (privado):
```typescript
private getMimeType(ext: string): string {
  const map: Record<string, string> = {
    '.pdf': 'application/pdf',
    '.png': 'image/png',
    '.jpg': 'image/jpeg',
    '.jpeg': 'image/jpeg',
    '.gif': 'image/gif',
    '.webp': 'image/webp',
    '.svg': 'image/svg+xml',
    '.txt': 'text/plain',
    '.json': 'application/json',
    '.csv': 'text/csv',
    '.html': 'text/html',
    '.md': 'text/markdown',
    '.zip': 'application/zip',
    '.mp4': 'video/mp4',
    '.mp3': 'audio/mpeg',
  };
  return map[ext.toLowerCase()] ?? 'application/octet-stream';
}
```

**Método extractOutboundAttachments** (privado):
```typescript
private extractOutboundAttachments(text: string): Array<{ name: string; url: string }> {
  // Acepta paths del contenedor (/workspace/group/files/...) y paths relativos (files/...)
  const regex = /(?:\/workspace\/group\/)?files\/([^\s\]"']+)/g;
  const attachments: Array<{ name: string; url: string }> = [];
  const seen = new Set<string>();

  let match;
  while ((match = regex.exec(text)) !== null) {
    const filename = match[1];
    if (seen.has(filename)) continue;
    seen.add(filename);

    const fullPath = path.join(this.filesDir, filename);
    if (fs.existsSync(fullPath)) {
      attachments.push({
        name: path.basename(filename),
        url: `/files/${filename}`,
      });
    }
  }

  return attachments;
}
```

**Modificar connect()** — llamar startFileServer() antes de crear el WSS (para que arranquen independientemente):
```typescript
async connect(): Promise<void> {
  // Start HTTP file server first (independent of WS)
  this.startFileServer();

  this.wss = new WebSocketServer({ port: this.port });
  // ... resto igual
```

**Modificar handleInboundMessage()** — rama `msg.type === 'chat'`:

Reemplazar la rama actual:
```typescript
if (msg.type === 'chat' && typeof msg.content === 'string') {
  const newMsg: NewMessage = {
    id: crypto.randomUUID(),
    chat_jid: WS_JID,
    sender: 'ws:user',
    sender_name: 'User',
    content: msg.content,
    timestamp: new Date().toISOString(),
  };
  this.opts.onMessage(WS_JID, newMsg);
}
```

Por:
```typescript
if (msg.type === 'chat') {
  // content puede estar vacío si el mensaje solo tiene attachments
  let content = (msg as { type: string; content?: string; attachments?: InboundAttachment[] }).content ?? '';

  const inboundMsg = msg as { type: string; content?: string; attachments?: InboundAttachment[] };
  if (Array.isArray(inboundMsg.attachments) && inboundMsg.attachments.length > 0) {
    const refs: string[] = [];
    for (const att of inboundMsg.attachments) {
      try {
        const relPath = this.saveInboundAttachment(att);
        refs.push(relPath);
      } catch (err) {
        logger.warn({ err, name: att.name }, 'Failed to save attachment');
      }
    }
    if (refs.length > 0) {
      const refBlock = refs.length === 1
        ? `\n\n[Attachment: ${refs[0]}]`
        : `\n\n[Attachments:\n${refs.map(r => `- ${r}`).join('\n')}]`;
      content = content + refBlock;
    }
  }

  // Solo procesar si hay content (texto o referencia de adjunto)
  if (content.length > 0) {
    const newMsg: NewMessage = {
      id: crypto.randomUUID(),
      chat_jid: WS_JID,
      sender: 'ws:user',
      sender_name: 'User',
      content,
      timestamp: new Date().toISOString(),
    };
    this.opts.onMessage(WS_JID, newMsg);
  }
}
```

NOTA: eliminar `typeof msg.content === 'string'` de la condición — un mensaje solo con attachments y content vacío es válido.

**Modificar sendMessage()** — añadir detección de adjuntos salientes:
```typescript
async sendMessage(_jid: string, text: string): Promise<void> {
  const attachments = this.extractOutboundAttachments(text);
  const payload: Record<string, unknown> = { type: 'chat', content: text };
  if (attachments.length > 0) {
    payload.attachments = attachments;
  }
  const serialized = JSON.stringify(payload);

  if (this.isConnected()) {
    this.client!.send(serialized);
  } else {
    // Buffer solo el text — al hacer flush, extractOutboundAttachments se re-ejecuta
    this.bufferMessage(text);
  }
}
```

**Modificar flushBuffer()** — re-detectar adjuntos salientes al enviar mensajes buffereados:
```typescript
private flushBuffer(ws: WebSocket): void {
  if (this.buffer.length === 0) return;

  const count = this.buffer.length;
  ws.send(JSON.stringify({ type: 'system', event: 'buffered_start', count }));

  for (const buffered of this.buffer) {
    const attachments = this.extractOutboundAttachments(buffered.content);
    const payload: Record<string, unknown> = {
      type: 'chat',
      content: buffered.content,
      timestamp: buffered.timestamp,
    };
    if (attachments.length > 0) {
      payload.attachments = attachments;
    }
    ws.send(JSON.stringify(payload));
  }

  ws.send(JSON.stringify({ type: 'system', event: 'buffered_end' }));
  this.buffer = [];
}
```

**Modificar disconnect()** — cerrar fileServer:
```typescript
async disconnect(): Promise<void> {
  if (this.heartbeatInterval) {
    clearInterval(this.heartbeatInterval);
    this.heartbeatInterval = null;
  }
  if (this.pongTimeout) {
    clearTimeout(this.pongTimeout);
    this.pongTimeout = null;
  }
  // Cerrar HTTP file server
  if (this.fileServer) {
    this.fileServer.close();
    this.fileServer = null;
  }
  if (this.wss) {
    this.wss.close();
    this.wss = null;
  }
  this.client = null;
}
```
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compila sin errores. websocket.ts contiene saveInboundAttachment, startFileServer, getMimeType, extractOutboundAttachments, y las modificaciones a connect(), handleInboundMessage(), sendMessage(), flushBuffer(), disconnect().</done>
</task>

<task type="auto">
  <name>Task 3: Tests para ATT-01, ATT-02, ATT-03</name>
  <files>src/channels/websocket.test.ts</files>
  <action>
Añadir nuevos bloques describe al final del archivo websocket.test.ts (antes del cierre del describe('WebSocketChannel') externo).

**Mock de node:fs y node:http** — añadir junto a los mocks existentes al inicio del archivo:

```typescript
// Mock node:fs — control sobre filesystem operations
vi.mock('node:fs', () => ({
  default: {
    mkdirSync: vi.fn(),
    writeFileSync: vi.fn(),
    existsSync: vi.fn().mockReturnValue(false),
    statSync: vi.fn(),
    createReadStream: vi.fn(),
  },
}));

// Mock node:http — interceptar createServer
const mockHttpServer = {
  on: vi.fn(),
  listen: vi.fn(),
  close: vi.fn(),
};
vi.mock('node:http', () => ({
  default: {
    createServer: vi.fn().mockReturnValue(mockHttpServer),
  },
}));

// Mock node:path — usar implementación real excepto sep
vi.mock('node:path', async () => {
  const actual = await vi.importActual<typeof import('node:path')>('node:path');
  return { default: actual };
});

// Mock node:crypto — randomBytes para nombres de archivo
vi.mock('node:crypto', async () => {
  const actual = await vi.importActual<typeof import('node:crypto')>('node:crypto');
  return { default: actual };
});
```

NOTA: Si los mocks de node:fs/http interfieren con los tests existentes, usar `vi.spyOn` selectivo dentro de cada describe block en lugar de mocks globales. Ajustar según lo que funcione con el runner vitest del proyecto.

**Constructor actualizado** — los tests existentes usan `new WebSocketChannel(3001, opts)`. El nuevo constructor tiene un tercer parámetro opcional `filesPort`. Actualizar las instancias en los tests de adjuntos a:
```typescript
new WebSocketChannel(3001, opts, 3002)
```

**Tests ATT-01 — adjuntos entrantes:**

```typescript
describe('inbound attachments (ATT-01)', () => {
  it('chat message with attachments saves files to disk and appends reference to content', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.mkdirSync.mockImplementation(() => undefined);
    mockFs.writeFileSync.mockImplementation(() => undefined);

    const onMessage = vi.fn();
    const channel = new WebSocketChannel(3001, { onMessage, onChatMetadata: vi.fn() }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);

    const attachment = {
      name: 'report.pdf',
      data: Buffer.from('fake pdf content').toString('base64'),
      mime: 'application/pdf',
      size: 16,
    };

    client._emit(
      'message',
      Buffer.from(JSON.stringify({ type: 'chat', content: 'here is the file', attachments: [attachment] })),
    );

    expect(mockFs.writeFileSync).toHaveBeenCalled();
    expect(onMessage).toHaveBeenCalledWith(
      'ws:better-work',
      expect.objectContaining({
        content: expect.stringContaining('[Attachment:'),
      }),
    );
    expect(onMessage).toHaveBeenCalledWith(
      'ws:better-work',
      expect.objectContaining({
        content: expect.stringContaining('here is the file'),
      }),
    );
  });

  it('chat message with only attachments (no content text) is processed', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.mkdirSync.mockImplementation(() => undefined);
    mockFs.writeFileSync.mockImplementation(() => undefined);

    const onMessage = vi.fn();
    const channel = new WebSocketChannel(3001, { onMessage, onChatMetadata: vi.fn() }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);

    const attachment = {
      name: 'photo.png',
      data: Buffer.from('fake image').toString('base64'),
      mime: 'image/png',
      size: 10,
    };

    client._emit(
      'message',
      Buffer.from(JSON.stringify({ type: 'chat', content: '', attachments: [attachment] })),
    );

    // onMessage should be called because content is non-empty after appending attachment ref
    expect(onMessage).toHaveBeenCalledWith(
      'ws:better-work',
      expect.objectContaining({
        content: expect.stringContaining('[Attachment:'),
      }),
    );
  });

  it('multiple attachments use [Attachments:...] block format', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.mkdirSync.mockImplementation(() => undefined);
    mockFs.writeFileSync.mockImplementation(() => undefined);

    const onMessage = vi.fn();
    const channel = new WebSocketChannel(3001, { onMessage, onChatMetadata: vi.fn() }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);

    client._emit(
      'message',
      Buffer.from(JSON.stringify({
        type: 'chat',
        content: 'two files',
        attachments: [
          { name: 'a.pdf', data: Buffer.from('a').toString('base64'), mime: 'application/pdf', size: 1 },
          { name: 'b.pdf', data: Buffer.from('b').toString('base64'), mime: 'application/pdf', size: 1 },
        ],
      })),
    );

    const call = onMessage.mock.calls[0][1];
    expect(call.content).toContain('[Attachments:');
    expect(call.content).toContain('- inbox/attachments/');
  });

  it('attachment save failure is caught and message continues with text', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.mkdirSync.mockImplementation(() => undefined);
    mockFs.writeFileSync.mockImplementation(() => { throw new Error('disk full'); });

    const onMessage = vi.fn();
    const channel = new WebSocketChannel(3001, { onMessage, onChatMetadata: vi.fn() }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);

    client._emit(
      'message',
      Buffer.from(JSON.stringify({
        type: 'chat',
        content: 'message with failed attachment',
        attachments: [{ name: 'x.pdf', data: 'dGVzdA==', mime: 'application/pdf', size: 4 }],
      })),
    );

    // Message should still be delivered with just the text (no attachment ref)
    expect(onMessage).toHaveBeenCalledWith(
      'ws:better-work',
      expect.objectContaining({ content: 'message with failed attachment' }),
    );
  });
});
```

**Tests ATT-02 — servidor HTTP estático:**

```typescript
describe('HTTP file server (ATT-02)', () => {
  it('startFileServer is called in connect()', async () => {
    const http = await import('node:http');
    const mockHttp = vi.mocked(http.default);
    mockHttp.createServer.mockReturnValue(mockHttpServer as unknown as http.Server);

    const channel = new WebSocketChannel(3001, {
      onMessage: vi.fn(),
      onChatMetadata: vi.fn(),
    }, 3002);
    await channel.connect();

    expect(mockHttp.createServer).toHaveBeenCalled();
    expect(mockHttpServer.listen).toHaveBeenCalledWith(3002, '127.0.0.1');
  });

  it('fileServer.close() called on disconnect()', async () => {
    const http = await import('node:http');
    const mockHttp = vi.mocked(http.default);
    mockHttp.createServer.mockReturnValue(mockHttpServer as unknown as http.Server);

    const channel = new WebSocketChannel(3001, {
      onMessage: vi.fn(),
      onChatMetadata: vi.fn(),
    }, 3002);
    await channel.connect();
    await channel.disconnect();

    expect(mockHttpServer.close).toHaveBeenCalled();
  });
});
```

**Tests ATT-03 — adjuntos salientes:**

```typescript
describe('outbound attachment detection (ATT-03)', () => {
  it('sendMessage includes attachments[] when text references files/ that exist', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    // Simulate files/report.pdf exists
    mockFs.existsSync.mockImplementation((p) =>
      String(p).endsWith('report.pdf') ? true : false,
    );

    const channel = new WebSocketChannel(3001, {
      onMessage: vi.fn(),
      onChatMetadata: vi.fn(),
    }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);
    client.send.mockClear();

    await channel.sendMessage('ws:better-work', 'Here is your report: files/report.pdf');

    const sentPayload = JSON.parse(client.send.mock.calls[0][0] as string);
    expect(sentPayload.attachments).toEqual([
      { name: 'report.pdf', url: '/files/report.pdf' },
    ]);
  });

  it('sendMessage does NOT include attachments[] when referenced file does not exist', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.existsSync.mockReturnValue(false);

    const channel = new WebSocketChannel(3001, {
      onMessage: vi.fn(),
      onChatMetadata: vi.fn(),
    }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);
    client.send.mockClear();

    await channel.sendMessage('ws:better-work', 'No file here: files/ghost.pdf');

    const sentPayload = JSON.parse(client.send.mock.calls[0][0] as string);
    expect(sentPayload.attachments).toBeUndefined();
  });

  it('sendMessage detects container path /workspace/group/files/ as well', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.existsSync.mockImplementation((p) =>
      String(p).endsWith('output.csv') ? true : false,
    );

    const channel = new WebSocketChannel(3001, {
      onMessage: vi.fn(),
      onChatMetadata: vi.fn(),
    }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);
    client.send.mockClear();

    await channel.sendMessage('ws:better-work', 'Saved to /workspace/group/files/output.csv');

    const sentPayload = JSON.parse(client.send.mock.calls[0][0] as string);
    expect(sentPayload.attachments).toEqual([
      { name: 'output.csv', url: '/files/output.csv' },
    ]);
  });

  it('sendMessage deduplicates repeated file references', async () => {
    const fs = await import('node:fs');
    const mockFs = vi.mocked(fs.default);
    mockFs.existsSync.mockReturnValue(true);

    const channel = new WebSocketChannel(3001, {
      onMessage: vi.fn(),
      onChatMetadata: vi.fn(),
    }, 3002);
    await channel.connect();
    const client = simulateClientConnect(channel);
    client.send.mockClear();

    await channel.sendMessage('ws:better-work', 'files/a.pdf and again files/a.pdf');

    const sentPayload = JSON.parse(client.send.mock.calls[0][0] as string);
    expect(sentPayload.attachments).toHaveLength(1);
  });
});
```

Ejecutar la suite completa después de añadir los tests. Si algún test falla por incompatibilidad de mocks (especialmente los mocks globales de node:fs/http que pueden interferir con tests existentes), ajustar el scope de los mocks usando `vi.spyOn` dentro de cada it() y restaurando con `vi.restoreAllMocks()` en afterEach. La prioridad es que los 25 tests existentes sigan pasando y los nuevos tests cubran los requirements.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && npx vitest run src/channels/websocket.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Todos los tests de websocket.test.ts pasan (los 25 originales + los nuevos de ATT-01, ATT-02, ATT-03). No hay regresiones.</done>
</task>

</tasks>

<verification>
Al completar el plan:

1. `cd /Users/danielmunoz/.nanoclaw && npx tsc --noEmit` — sin errores TypeScript
2. `cd /Users/danielmunoz/.nanoclaw && npx vitest run src/channels/websocket.test.ts` — todos los tests pasan
3. `grep -n "WEBSOCKET_FILES_PORT" /Users/danielmunoz/.nanoclaw/src/config.ts` — muestra la constante exportada
4. `grep -n "saveInboundAttachment\|startFileServer\|extractOutboundAttachments" /Users/danielmunoz/.nanoclaw/src/channels/websocket.ts` — los tres métodos existen
</verification>

<success_criteria>
- CONF-03: `WEBSOCKET_FILES_PORT` exportado desde src/config.ts, env var configurable, default 3002
- ATT-01: Mensajes WS con attachments[] guardan archivos en inbox/attachments/ y añaden referencia al content del agente
- ATT-02: Servidor HTTP estático arranca en connect(), sirve files/ con path traversal protection y CORS, no crashea si el puerto está ocupado
- ATT-03: sendMessage() detecta referencias files/ en el texto del agente y las incluye como attachments[{name, url}] en el mensaje WS saliente
- Todos los tests pasan (sin regresiones en los 25 tests de Phase 1)
</success_criteria>

<output>
After completion, create `/Users/danielmunoz/.nanoclaw/.planning/phases/02-attachments/02-01-SUMMARY.md`
</output>
