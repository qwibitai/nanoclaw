---
phase: 01-websocket-channel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/config.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
must_haves:
  truths:
    - "El proyecto compila sin errores de TypeScript relacionados con ws"
    - "Las constantes WEBSOCKET_ENABLED y WEBSOCKET_PORT están disponibles en src/config.ts"
    - "ws y @types/ws son dependencias directas en package.json"
  artifacts:
    - path: "package.json"
      provides: "ws como dependencia directa, @types/ws como devDependency"
      contains: "\"ws\":"
    - path: "src/config.ts"
      provides: "WEBSOCKET_ENABLED y WEBSOCKET_PORT exportadas"
      exports: ["WEBSOCKET_ENABLED", "WEBSOCKET_PORT"]
  key_links:
    - from: "src/channels/websocket.ts"
      to: "src/config.ts"
      via: "import WEBSOCKET_PORT"
      pattern: "WEBSOCKET_PORT"
---

<objective>
Añadir ws como dependencia directa y exponer las constantes de configuración WEBSOCKET_ENABLED y WEBSOCKET_PORT que el canal WebSocket necesita.

Purpose: ws ya está instalada como transitiva de Baileys. Sin declararla como dependencia directa, puede desaparecer en una actualización. Las constantes de config son el contrato que conecta el .env con el canal.
Output: package.json actualizado, src/config.ts con dos nuevas exportaciones.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Patrón existente en src/config.ts que debe seguirse exactamente -->

<!-- readEnvFile lee las claves del array y las combina con process.env -->
<!-- La constante boolean usa !== 'false' (WEBSOCKET_ENABLED sigue este patrón: default true) -->
<!-- La constante numérica usa parseInt con fallback a string -->

Patrón existente (src/config.ts líneas 9-14 y 72-79):
```typescript
const envConfig = readEnvFile([
  'ASSISTANT_NAME',
  'TELEGRAM_BOT_TOKEN',
  // añadir 'WEBSOCKET_ENABLED', 'WEBSOCKET_PORT' aquí
]);

// Boolean con default true:
// (process.env.FOO ?? envConfig.FOO ?? 'true') !== 'false'

// Integer con default:
// parseInt(process.env.FOO || envConfig.FOO || '3001', 10)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Añadir ws y @types/ws como dependencias directas</name>
  <files>package.json</files>
  <action>
    Ejecutar `pnpm add ws` y `pnpm add -D @types/ws` en el directorio del proyecto (/Users/danielmunoz/.nanoclaw).

    ws ya está instalada como transitiva de @whiskeysockets/baileys (v8.19.0). pnpm add la promoverá a dependencia directa en package.json sin reinstalar. @types/ws no está instalada — pnpm la instalará.

    No añadir ninguna otra dependencia. No modificar ningún otro archivo.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && node -e "import('ws').then(m => console.log('ws ok:', typeof m.default))" 2>&1 && grep '"ws"' package.json && grep '@types/ws' package.json</automated>
  </verify>
  <done>package.json tiene "ws" en dependencies y "@types/ws" en devDependencies. El import de ws funciona en Node.js.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir WEBSOCKET_ENABLED y WEBSOCKET_PORT a src/config.ts</name>
  <files>src/config.ts</files>
  <action>
    Modificar src/config.ts para:

    1. Añadir 'WEBSOCKET_ENABLED' y 'WEBSOCKET_PORT' al array de readEnvFile (junto a las otras claves existentes).

    2. Añadir al final del archivo estas dos exportaciones siguiendo el patrón existente:

    ```typescript
    export const WEBSOCKET_ENABLED =
      (process.env.WEBSOCKET_ENABLED ?? envConfig.WEBSOCKET_ENABLED ?? 'true') !== 'false';
    export const WEBSOCKET_PORT = parseInt(
      process.env.WEBSOCKET_PORT || envConfig.WEBSOCKET_PORT || '3001',
      10,
    );
    ```

    No modificar ninguna otra constante. No añadir comentarios.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build pasa sin errores. WEBSOCKET_ENABLED y WEBSOCKET_PORT están exportadas en src/config.ts.</done>
</task>

</tasks>

<verification>
- `npm run build` pasa sin errores TypeScript
- `grep '"ws"' package.json` muestra la dependencia directa
- `grep 'WEBSOCKET_ENABLED\|WEBSOCKET_PORT' src/config.ts` muestra ambas exportaciones
</verification>

<success_criteria>
El proyecto compila, ws es dependencia directa, y las dos constantes de config están exportadas y listas para ser importadas por src/channels/websocket.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-websocket-channel/01-01-SUMMARY.md`
</output>
