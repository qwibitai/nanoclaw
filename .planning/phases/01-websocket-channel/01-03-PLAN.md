---
phase: 01-websocket-channel
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/index.ts
autonomous: false
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
must_haves:
  truths:
    - "NanoClaw arranca con el canal WS escuchando en el puerto configurado cuando WEBSOCKET_ENABLED=true"
    - "El grupo better-work existe en la DB y en groups/better-work/ al arrancar sin intervención manual"
    - "Si WEBSOCKET_ENABLED=false, el canal WS no se registra y NanoClaw arranca normalmente"
    - "groups/better-work/CLAUDE.md existe con el contenido definido en CONTEXT.md"
  artifacts:
    - path: "src/index.ts"
      provides: "Registro condicional del canal WS y auto-provisión del grupo better-work"
      contains: "WebSocketChannel"
    - path: "groups/better-work/CLAUDE.md"
      provides: "Memoria inicial del agente better-work"
      contains: "asistente de productividad"
  key_links:
    - from: "src/index.ts main()"
      to: "WebSocketChannel"
      via: "if (WEBSOCKET_ENABLED) { new WebSocketChannel(WEBSOCKET_PORT, channelOpts) }"
      pattern: "WEBSOCKET_ENABLED"
    - from: "src/index.ts main()"
      to: "registerGroup / setRegisteredGroup"
      via: "ensureBetterWorkGroup() antes del message loop"
      pattern: "ensureBetterWorkGroup"
---

<objective>
Registrar el canal WebSocket en main() condicionado a WEBSOCKET_ENABLED y auto-provisionar el grupo better-work en la DB y en disco.

Purpose: Sin esta integración, el canal existe pero nunca se activa. El grupo better-work debe existir en DB antes del primer mensaje para evitar FK constraints.
Output: src/index.ts modificado con el canal WS y la función ensureBetterWorkGroup.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-websocket-channel/01-CONTEXT.md
@.planning/phases/01-websocket-channel/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos existentes en src/index.ts que la integración debe seguir -->

<!-- channelOpts — ya definido en main(), pasar directamente al WebSocketChannel -->
```typescript
const channelOpts = {
  onMessage: (_chatJid: string, msg: NewMessage) => storeMessage(msg),
  onChatMetadata: (chatJid, timestamp, name?, channel?, isGroup?) =>
    storeChatMetadata(chatJid, timestamp, name, channel, isGroup),
  registeredGroups: () => registeredGroups,
};
```
NOTA: WebSocketChannelOpts solo necesita onMessage y onChatMetadata (no registeredGroups).

<!-- registerGroup — función privada de index.ts, crea el directorio y actualiza DB -->
```typescript
function registerGroup(jid: string, group: RegisteredGroup): void {
  // Crea groupDir/logs/, llama setRegisteredGroup(jid, group)
}
```

<!-- RegisteredGroup — tipo de src/types.ts -->
```typescript
export interface RegisteredGroup {
  name: string;
  folder: string;
  trigger: string;
  added_at: string;
  containerConfig?: ContainerConfig;
  requiresTrigger?: boolean;
}
```

<!-- Patrón condicional Telegram (líneas 481-490 de src/index.ts) -->
```typescript
if (TELEGRAM_BOT_TOKEN) {
  const telegram = new TelegramChannel(TELEGRAM_BOT_TOKEN, channelOpts);
  channels.push(telegram);
  await telegram.connect();
}
```

<!-- resolveGroupFolderPath — para obtener la ruta absoluta del grupo -->
```typescript
import { resolveGroupFolderPath } from './group-folder.js';
// Uso: resolveGroupFolderPath('better-work') → ruta absoluta a groups/better-work
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Registrar WebSocketChannel y auto-provisionar better-work en src/index.ts</name>
  <files>src/index.ts</files>
  <action>
    Modificar src/index.ts para añadir la integración del canal WS. Cambios necesarios:

    **1. Añadir import del canal:**
    ```typescript
    import { WebSocketChannel } from './channels/websocket.js';
    ```
    Añadir esta línea junto a los imports de otros canales (cerca de WhatsAppChannel, TelegramChannel).

    **2. Añadir WEBSOCKET_ENABLED y WEBSOCKET_PORT al import de config:**
    ```typescript
    import {
      ASSISTANT_NAME,
      IDLE_TIMEOUT,
      MAIN_GROUP_FOLDER,
      POLL_INTERVAL,
      TELEGRAM_BOT_TOKEN,
      TELEGRAM_BOT_POOL,
      TELEGRAM_ONLY,
      TRIGGER_PATTERN,
      WEBSOCKET_ENABLED,
      WEBSOCKET_PORT,
    } from './config.js';
    ```

    **3. Añadir constante con el contenido del CLAUDE.md inicial** (antes de main()):
    ```typescript
    const BETTER_WORK_CLAUDE_MD = `# better-work

    Eres un asistente de productividad personal.

    ## Idioma
    Responde siempre en español. Sin excepciones para las respuestas al usuario.
    Puedes leer y escribir en catalán e inglés cuando el usuario lo pida explícitamente (emails, documentos, traducciones). Nunca lo hagas de forma espontánea.

    ## Tono
    Directo y conciso. Sin relleno, sin frases de cortesía vacías.

    ## Filesystem
    Tienes acceso al filesystem del grupo en /workspace/group/.
    `;
    ```

    **4. Añadir función ensureBetterWorkGroup** (antes de main(), después de BETTER_WORK_CLAUDE_MD):
    ```typescript
    function ensureBetterWorkGroup(): void {
      const jid = 'ws:better-work';
      if (registeredGroups[jid]) return;

      registerGroup(jid, {
        name: 'better-work',
        folder: 'better-work',
        trigger: '@Nano',
        added_at: new Date().toISOString(),
        requiresTrigger: false,
      });

      const groupDir = resolveGroupFolderPath('better-work');
      const claudeMdPath = path.join(groupDir, 'CLAUDE.md');
      if (!fs.existsSync(claudeMdPath)) {
        fs.writeFileSync(claudeMdPath, BETTER_WORK_CLAUDE_MD);
      }
    }
    ```

    **5. En main(), después del bloque condicional de Telegram, añadir:**
    ```typescript
    if (WEBSOCKET_ENABLED) {
      const wsChannel = new WebSocketChannel(WEBSOCKET_PORT, {
        onMessage: channelOpts.onMessage,
        onChatMetadata: channelOpts.onChatMetadata,
      });
      channels.push(wsChannel);
      await wsChannel.connect();
      ensureBetterWorkGroup();
    }
    ```

    El bloque debe ir DESPUÉS del bloque de Telegram y ANTES de startSchedulerLoop().
    ensureBetterWorkGroup() se llama después de connect() porque initDatabase() ya fue llamado al inicio de main().

    **No modificar** ningún otro código de main(), registeredGroups, channelOpts, ni ninguna otra función existente.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build pasa sin errores. grep 'WebSocketChannel\|WEBSOCKET_ENABLED\|ensureBetterWorkGroup' src/index.ts muestra los tres términos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar integración completa del canal WS</name>
  <what-built>
    Canal WebSocket completo integrado en NanoClaw:
    - ws y @types/ws como dependencias directas en package.json
    - WEBSOCKET_ENABLED y WEBSOCKET_PORT en src/config.ts
    - src/channels/websocket.ts con protocolo completo, buffer de 50 msgs, heartbeat 30s/10s
    - src/channels/websocket.test.ts con tests de comportamiento
    - src/index.ts registra el canal condicionado a WEBSOCKET_ENABLED y auto-provisionando better-work
  </what-built>
  <how-to-verify>
    1. Verificar que los tests pasan:
       ```bash
       cd ~/.nanoclaw && npx vitest run src/channels/websocket.test.ts
       ```

    2. Verificar que el build es limpio:
       ```bash
       npm run build
       ```

    3. Smoke test del canal WS (requiere que WhatsApp NO esté autenticado para evitar errores, o usar TELEGRAM_ONLY=true):
       ```bash
       TELEGRAM_ONLY=true node dist/index.js &
       node -e "
         const { WebSocket } = require('ws');
         const ws = new WebSocket('ws://localhost:3001');
         ws.on('open', () => {
           console.log('Connected');
           ws.send(JSON.stringify({type:'chat', content:'hola mundo'}));
         });
         ws.on('message', (d) => console.log('Received:', d.toString()));
         setTimeout(() => { ws.close(); process.exit(0); }, 3000);
       "
       ```
       Esperado: recibir `{"type":"system","event":"connected","payload":{"buffered_count":0}}` en la consola.

    4. Verificar que groups/better-work/CLAUDE.md existe:
       ```bash
       cat ~/.nanoclaw/groups/better-work/CLAUDE.md
       ```
  </how-to-verify>
  <action>Verificación manual de la integración completa del canal WebSocket.</action>
  <verify>
    <automated>cd /Users/danielmunoz/.nanoclaw && npx vitest run src/channels/websocket.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Tests pasan, build limpio, cliente WS recibe evento "connected", groups/better-work/CLAUDE.md existe con contenido correcto.</done>
  <resume-signal>Escribe "approved" si todo funciona, o describe cualquier problema encontrado.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` pasa sin errores TypeScript
- `npx vitest run` pasa todos los tests del proyecto
- `groups/better-work/CLAUDE.md` existe con contenido correcto tras arrancar
- Cliente WS puede conectar al puerto 3001 y recibe evento "connected"
</verification>

<success_criteria>
NanoClaw arranca con el canal WS activo en el puerto 3001. El grupo better-work existe en DB y en disco. Un cliente WS puede conectar y recibir el handshake de bienvenida. Todos los tests pasan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-websocket-channel/01-03-SUMMARY.md`
</output>
