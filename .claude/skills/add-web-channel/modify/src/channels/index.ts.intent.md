# Modify `src/channels/index.ts`

## What this change adds

Adds the web channel barrel import:

```ts
import './web.js';
```

## Invariant

Keep existing channel comment sections and imports intact; only append the new web import.
