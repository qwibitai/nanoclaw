# Farm Digest Template

## Farm Snapshot

### Ready To Launch (Approved)
- `<ISSUE_ID>` (`<REPO>`): `<title>`

### In Progress (Coding)
- `<ISSUE_ID>` (`<REPO>`): phase=`<phase>`, update_age=`<age>`, session=`alive|dead`

### Ready For Review
- `<ISSUE_ID>` (`<REPO>`): `<PR_URL or missing>`

### Warnings
- `<source>`: `<error>`

### Actions Needed
1. Launch: `<issue list>`
2. Review: `<issue/pr list>`
3. Merge: `<pr list>`
