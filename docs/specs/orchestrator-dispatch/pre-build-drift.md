# Pre-Build Drift Report — orchestrator-dispatch

> Generated by `/team-drift` — 2026-05-09 (Step 2 of `/team-build`)
> SOT: `docs/specs/orchestrator-dispatch/design.md` (cycle-3 user-approved revision)
> Target: `docs/specs/orchestrator-dispatch/plan.md` (post-revision-1; B3 + D5 inline-revised this run)
> Reviewers: Agent A (Claude Sonnet, general-purpose) + Agent B (Codex GPT-5.5, xhigh reasoning, read-only sandbox)

---

## Summary

| Class      | Count | Blocking? |
|------------|-------|-----------|
| CONFIRMED  | 86    | No        |
| PARTIAL    | 11    | Review    |
| MISSING    | 0     | —         |
| DIVERGED   | 1 (acked) | No |

**Effective DIVERGED:** 0 (1 entry acknowledged — see drift-acks.json)
**Gate:** ✅ PASS — `MISSING == 0 && effective_DIVERGED == 0`

---

## Methodology

Two independent agents extracted claims from the SOT and verified each against the target. The team lead merged numbered claim lists, reconciled verdicts, fact-checked disputed verdicts directly against design.md, and applied two surgical plan revisions inline (within Stage B's 1/1 revision cap) to address two substantive findings (Codex #43, Codex #56) before classifying the remaining residual.

**Total claims (post-merge):** 102 unique claims across the two agents
**Sources:**
- design.md §1 (modules), §2 (schema), §3 (MCP tools), §4 (system actions), §5 (watchdog), §6 (adapters), §7 (Phase 2 dashboard), §"MVP Scope Boundary"
- decisions.yaml constraints C1-C21, decisions D5-D17, rejected options D1-Option-B and D1-Option-C, review_cycles[3]

---

## DIVERGED — Acknowledged (1)

### [B1] Bifurcated mounting mutual-exclusion (Codex #24)

**Class:** DIVERGED
**Severity:** Low (acked)
**Acknowledged in:** `drift-acks.json` (id: B1)

**SOT claim** (design.md line 313): "These two conditions are NOT mutually exclusive — an orchestrator session that itself was dispatched by a higher-level orchestrator (theoretical; not a Phase 1 use case but the schema supports it) would mount both sets."

**Plan target** (plan.md D3, lines 1026 + 1032): "register orchestrator-only tools ONLY if `getSessionDispatchTaskId() === null` AND the agent's group has orchestrator capability" + ASSERT: "bifurcated mounting: when `getSessionDispatchTaskId() !== null`, orchestrator tools are NOT mounted".

**Why this is acceptable:** Design itself flags the orchestrator-of-orchestrator scenario as "not a Phase 1 use case." The plan's mutual-exclusion is a Phase 1 simplification consistent with design's own scope carve-out. Schema-level support (capability table + `session_routing.dispatch_task_id` column) is unchanged, so Phase 2 can relax the mounting condition with a one-line code change without schema churn. Risk to Phase 1: zero — the only "blocked" use case is design-acknowledged as non-Phase-1.

---

## MISSING — Resolved Inline (0)

Both substantive MISSING findings were resolved by surgical plan revisions during this drift triage (Stage B revision-cycle 1/1):

### Resolved during triage

**Codex #43 — `adapter_unavailable` specific branch in `completeDispatchSideEffects`**
- **SOT:** design.md line 437 — "If reconciler observes `surface_mode='native_thread'` but `getChannelAdapter(channel_type).createThread` is undefined: mark `failed` with `fail_reason='adapter_unavailable'`"
- **Original gap:** plan B3 only had generic `completion_exhausted` after 5 retries — burned retry budget on a permanent condition
- **Resolution:** added ASSERT to B3 + named test `test_adapter_unavailable_marks_failed_immediately` (with `dispatch_completion_attempts === 0` assertion to lock in retry-budget bypass)
- **Auto judgment recorded:** decisions.yaml `auto_judgments` Build iteration 1

**Codex #56 — `_dispatch_cancel` envelope formatter handling**
- **SOT:** design.md §4 S26 — "cancellation handler writes structured envelope to child's inbound; child formatter renders it as a system note for graceful flush"
- **Original gap:** plan D5 only handled `_dispatch` envelope (session start), not `_dispatch_cancel` (mid-session cancellation)
- **Resolution:** extended D5 implementation approach + ASSERTs + 2 new test cases (`test_dispatch_cancel_envelope_renders_as_system_note`, `test_dispatch_cancel_envelope_without_reason`) + new acceptance criterion
- **Auto judgment recorded:** decisions.yaml `auto_judgments` Build iteration 1

### Non-blocking by SOT itself

**Agent A #5 — `registerDeliveryAction` duplicate-key throw**
- **SOT:** design.md §1 — "An additional hardening step should extend `registerDeliveryAction` ... noted as follow-up, not blocking"
- **Classification:** SOFT improvement. The SOT explicitly disclaims it as non-blocking ("noted as follow-up, not blocking"). Not a target-must-have; informational only. Carry forward to a separate hardening sprint as a known follow-up.

---

## PARTIAL (11)

### By-design Phase 2 deferrals (8 — not drift)

The plan explicitly scopes Phase 2 (read-only dashboard) for a separate `/team-plan` invocation. The following design-§7 details are intentionally not specified in this Phase 1 plan; they will be specified in the Phase 2 plan.

| ID | Topic | SOT location | Carry forward |
|----|-------|--------------|---------------|
| P1 | Vite+React SPA implementation detail | §7 + Codex #6 | Phase 2 plan |
| P2 | Memory reconciliation gate procedure (`ps -o rss=`, 60-min, 5-task) | §7 + Codex #7 | Phase 2 plan; tracked as Known Risk K4 |
| P3 | Dashboard auth via `canAccessAgentGroup` / `user_roles` | §7 + Codex #10 | Phase 2 plan |
| P4 | Phase 2 owner-cancel write path (Codex #57) | §4 + §7 | Phase 2 plan; current Phase 2 framing is read-only at launch |
| P5 | Specific dashboard route table | §7 + Codex #68 | Phase 2 plan |
| P6 | DM-token exchange detail + httpOnly Secure cookie | §7 + Codex #69 | Phase 2 plan |
| P7 | SSE chokidar watch paths + 200ms debounce | §7 + Codex #70 | Phase 2 plan |
| P8 | SWR / EventSource / hash-tab UI detail | §7 + Codex #71 | Phase 2 plan |

### Substantive PARTIAL — review required (3)

**[P9] Codex #26 — Container parent_session_id source**
- **SOT:** §3 implies `parent_session_id` comes from session_routing
- **Plan target:** D3 line 1023 references `NANOCLAW_SESSION_ID` mechanism
- **Gap:** the plan's reference is tentative ("verify mount path at /team-build")
- **Resolution path:** acceptance criterion in D3 — builder must verify the mount and use the canonical session-routing read; if env-var-based, verify the env injection path is wired by container-runner.ts. Track as a build-time decision; no schema change.

**[P10] Codex #34 — target validation completeness**
- **SOT:** §4 step 4 — "Reject when target_group has agent_group_capabilities.role = 'orchestrator'"
- **Plan target:** B2 ASSERT covers the orchestrator-target rejection
- **Gap:** plan does not assert (a) target_agent_group exists at all, or (b) target ≠ caller's own group (no self-dispatch)
- **Resolution path:** builder adds these as defensive validations during B2 implementation; current `target_group` foreign-key constraint in tasks table catches (a) at INSERT-time; (b) is a SOFT improvement — track as Known Risk addition K6.

**[P11] Codex #38 — `wakeContainer(callerSession)` after admit notification**
- **SOT:** §4 — admit notification path
- **Plan target:** B2 wraps writeSessionMessage to caller in try/catch (line 497); does not explicitly call wakeContainer(caller)
- **Gap:** orchestrator might miss the admit-result message until next inbound activity
- **Resolution path:** SHOULD-FIX during build — add `wakeContainer(callerSession)` after the writeSessionMessage. Builder applies as part of B2 implementation; verified by integration test.

---

## Known Risks Updated

This pre-build drift triage adds the following to plan.md Known Risks section (carry-forward to Phase 2 / build):

- **K6 (new) — Self-dispatch validation:** plan B2 doesn't explicitly reject `target_agent_group_id === caller_agent_group_id`. Foreign-key constraint enforces target exists; self-dispatch is allowed by current spec. Builder may add a defensive check during implementation; if added, document as a behavior change in build-state.md.

---

## Carry-forward to /team-build

| Item | Owner | Action |
|------|-------|--------|
| B1 (bifurcated mounting mutual-exclusion) | drift-acks.json | Acknowledged; gate passes |
| Codex #43 fix in B3 | Group B builder | Implement adapter_unavailable check + new test (already in plan) |
| Codex #56 fix in D5 | Group D builder | Implement _dispatch_cancel envelope rendering + 2 new tests (already in plan) |
| P9 (parent_session_id source) | Group D builder | Verify env-var or session-routing read at build time |
| P10 (target validation completeness) | Group B builder | Defensive existence + self-dispatch checks at B2 |
| P11 (wakeContainer after admit) | Group B builder | Add wakeContainer(callerSession) after writeSessionMessage |
| Phase 2 deferrals P1-P8 | Phase 2 `/team-plan` | Specified there, not here |

---

## Reviewer outputs (raw)

- Agent A (Claude Sonnet) extraction: 100 claims, 92 CONFIRMED / 7 PARTIAL / 1 MISSING / 0 DIVERGED — see Step 2 transcript
- Agent B (Codex GPT-5.5 xhigh) extraction: 86 claims, ~67 CONFIRMED / 17 PARTIAL / 1 MISSING / 2 DIVERGED — see `.claude/tmp/drift-codex.log` lines 2700-3316

Discrepancies between the two agents resolved by team-lead reading of design.md sections explicitly cited in disputed verdicts (lines 291-298 for allow-list reasoning; lines 313 for bifurcated mounting; lines 398, 437 for adapter_unavailable; §4 S26 for `_dispatch_cancel` formatter handling).

---

## Gate

```
MISSING:  0
DIVERGED: 1 (1 acked, 0 effective)
PARTIAL:  11 (8 by-design Phase 2 deferral, 3 substantive — folded into builder acceptance)
CONFIRMED: 86

Gate condition: MISSING == 0 && effective_DIVERGED == 0 → ✅ PASS
```

Build may proceed to Step 3 (TeamCreate + builder spawn).
