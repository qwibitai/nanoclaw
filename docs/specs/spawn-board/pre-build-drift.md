# Pre-Build Drift Report: Spawn Board (Design → Plan)

> Generated by `/team-drift` — 2026-05-10
> SOT: `docs/specs/spawn-board/design.md` (cycle-3 revised, 1028 lines)
> Target: `docs/specs/spawn-board/plan.md` (2482 lines, after small post-drift fix for echo_status enumeration)

## Summary

| Class | Count | Notes |
|-------|-------|-------|
| CONFIRMED | ~190+ | All HARD/SOFT constraints (C1-C12), all 9 design assumptions (A1-A9), all 12 rejections (R1-R12), all design decisions (D1-D5, D7-D15), all cycle-1/2/3 fixes traced to specific tasks. Constraint Traceability matrix in plan.md is the authoritative cross-reference. |
| PARTIAL | ~7 | Minor (e.g., A1 binary gate not enumerated in Integration Checklist; SPA bundle gzip check not enumerated; full /team-ship 60min procedure not transcribed verbatim into plan body). All low-impact / cosmetic. |
| DIVERGED | 3 | All intentional or design-side stale wording — see acks below |
| MISSING | 1 (substantive) + 2 (out-of-scope items) | Substantive issue addressed via small plan amendment |

**Two extractor agents ran in parallel:** Agent A (Claude Sonnet via general-purpose subagent) and Agent B (Codex `gpt-5.5` via `codex exec --yolo` with `read-only` sandbox + `xhigh` reasoning). Codex's stricter extraction (286 claims) was the basis for the consolidated findings below; Agent A reported 0 DIVERGED / 0 MISSING with 201 claims (including a final summary line).

---

## Findings

### B1 — DIVERGED — Claim #15: Vite pin (intentional)

- **SOT (design §Research):** "vite 8.0.11 — latest stable."
- **Target (plan E1):** Pinned to `vite@^8.0.10` with the note "NOT `8.0.11` per S6-c1 — borderline minimumReleaseAge."
- **Reason for divergence:** Cycle-1 review finding S6 (W4 cycle 1 / W4-c1) — Vite 8.0.11 was 3 days old at design time, exactly at the `minimumReleaseAge: 4320` threshold. Pinning ^8.0.10 (one patch back) avoids the borderline case. This is intentional; the plan correctly applies the cycle-1 finding.
- **Class:** DIVERGED
- **Resolution:** Acknowledged in `drift-acks.json`. No plan change.

### B2 — DIVERGED — Claim #59: Member-role sessions visibility (design internal inconsistency)

- **SOT:** Design §2a table for `GET /dashboard/api/sessions` Member column reads "empty" (4th column header).
- **SOT (also):** Design §2a body paragraph for sessions endpoint says member sees their groups read-only.
- **Target (plan D4 + D6):** Plan resolves in favor of "Member sees only their groups (read-only — no steer affordance)".
- **Reason for divergence:** Design has internal inconsistency (table column says empty; prose says read-only-with-groups). Plan picked the more useful interpretation. A member who is in an agent group should see live sessions for that group; otherwise the dashboard is unusable for member-level admins.
- **Class:** DIVERGED
- **Resolution:** Acknowledged in `drift-acks.json` with reason. No plan change. Design table column 4 should be updated to "their groups (read-only)" in a future cycle for consistency, but does not block build.

### B3 — DIVERGED — Claim #134: chokidar glob watch (design-side stale A4 wording)

- **SOT:** Design assumption A4 references `data/v2-sessions/*/*/outbound.db` glob.
- **SOT (also):** Design §5 cycle-2-revised explicitly forbids glob watch ("chokidar v4+ removed glob support") and uses directory watch + `ignored` filter.
- **Target (plan D1):** Plan correctly uses directory watch + `ignored` filter per §5.
- **Reason for "divergence":** Misclassification by Codex. The plan correctly follows §5 cycle-2-revised. The A4 text in the assumptions log still mentions the glob shape but the validation method is consistent with §5. The plan tracks the design's authoritative section (§5), not the secondary mention in A4.
- **Class:** Misclassified DIVERGED; functionally CONFIRMED.
- **Resolution:** Acknowledged in `drift-acks.json` with reason. Design A4 wording should be updated in a future cycle for consistency.

### B4 — Originally MISSING — Claim #95: echo_status enumeration

- **SOT:** Design §4 step 6 enumerates 4 echo outcome statuses (`echoed`, `echo_failed`, `adapter_unavailable`, `skipped_headless`).
- **Target (plan D5 BEFORE fix):** Plan D5 ASSERTs covered `echoed` and `echo_failed` paths but did not explicitly enumerate `adapter_unavailable` or `skipped_headless` as required emit values.
- **Resolution applied 2026-05-10 (small plan amendment):** Added an explicit ASSERT to plan Task D5: *"the SSE `task_event` emitted by `fireEchoAsync` MUST carry one of these exact status values: `'echoed'`, `'echo_failed'`, `'adapter_unavailable'`, or `'skipped_headless'`. Tests verify each path emits the correct status."* This is a 1-paragraph addition; it does not change task structure or scope.
- **Class:** Originally MISSING; **resolved by amendment** before this report finalized. Now CONFIRMED.

### B5 — MISSING (out-of-scope) — Claim #129: token-revocation UI deferred

- **SOT:** Design's MVP Scope Boundary lists "Token revocation UI" under OUT of scope (deferred to Phase 2B).
- **Target:** Plan does not explicitly trace this rejection.
- **Class:** MISSING (out-of-scope items)
- **Resolution:** Acknowledged in `drift-acks.json`. Out-of-scope items don't require plan traces because the plan IS the scope boundary; absence of the feature in the plan is sufficient evidence that it's out of scope. Acked.

### B6 — MISSING (out-of-scope) — Claim #130: HTTP/2 SSE multiplexing rejected

- **SOT:** Design's MVP Scope Boundary lists "Server-Sent Events with HTTP/2 multiplexing" under OUT of scope (HTTP/1.1 long-polling SSE).
- **Target:** Plan does not explicitly trace this rejection.
- **Class:** MISSING (out-of-scope items)
- **Resolution:** Same as B5 — acked.

---

## Effective Counts (after acks + plan amendment)

| Class | Raw | Effective |
|-------|-----|-----------|
| MISSING (substantive) | 1 | 0 (B4 resolved by plan amendment) |
| MISSING (out-of-scope) | 2 | 0 (B5, B6 acked) |
| DIVERGED | 3 | 0 (B1, B2, B3 acked) |
| **Effective blocking** | — | **0** |

---

## Gate

**Drift check complete.**

SOT: `docs/specs/spawn-board/design.md`
Target: `docs/specs/spawn-board/plan.md`

MISSING: 1 (resolved by amendment) + 2 (out-of-scope, acked) → **0 effective**
DIVERGED: 3 (all acked with reasons in `drift-acks.json`) → **0 effective**
PARTIAL: 7 — non-blocking, low-impact (catalogued in plan.md Integration Checklist)
CONFIRMED: ~190+

**Effective MISSING == 0 AND effective DIVERGED == 0. Pre-build drift gate clears.**

The plan tracks the cycle-3-revised design with very high fidelity. The Constraint Traceability matrix in plan.md provides authoritative cross-reference.
